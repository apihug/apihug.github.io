export const title = "ApiHug UI Protocol";
export const description =
  "Proto protocol extensions and explanation";

## Protocol

The protocol, which is the ProtoBuffer DSL, remains unchanged and is still the same as the previous [Proto DSL](/docs/protobuf)
